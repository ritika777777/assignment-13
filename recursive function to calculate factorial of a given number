#include<stdio.h>
int factorial(int);
int main(){
int num,f;
printf("enter number: ");
scanf("%d",&num);
f=factorial(num);
printf("%d",f);
return 0;
}

int factorial(int num){
int fact=1,i=1;
for(num;num>=i;num--){
    fact=fact*num;
}
return fact;

}
