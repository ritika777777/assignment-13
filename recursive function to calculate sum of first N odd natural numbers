#include<stdio.h>
int add(int);
int main(){
int num;
printf("enter number: ");
scanf("%d",&num);
add(num);
return 0;
}

int add(int num){
int i,sum=0;
for(i=1;i<=num;i++){
if(i%2 != 0 || i==2)
sum=sum+i;
}
return sum;

}

